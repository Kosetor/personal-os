# 🤖 Инструкция для hermes_b: репозиторий Personal OS Learning

> hermes_b — второй агент владельца. Этот файл — твои правила для работы с
> репозиторием Kosetor/personal-os. Репозиторий сменил назначение: больше НЕ
> дашборд с агентами, а система обучения управленческим навыкам.

## 1. Важное изменение (сентябрь 2026)

**Старый дашборд удалён** (архив — ветка `archive-dashboard` в этом же репо).
Мосты bridge/funnel и Tailscale Funnel больше НЕ нужны для этого сайта.
Не пытайся чинить config.js / app.js / PIN-авторизацию — их больше нет.

## 2. Текущее назначение репозитория

Публикация статического обучающего сайта «Personal OS Learning»:
- статья дня + адаптивный тест (FSRS) + граф тем прогрессии;
- приватные данные — в `~/personal-os-private/` (learning.db, источники), ВНЕ git;
- в git — только обезличенная статика в `site/`.

## 3. Твои задачи здесь

Если владелец просит тебя участвовать в обучающей системе:
- помогать с генерацией/редактурой статей (черновики → `content/drafts/`);
- проверять факты/нормативы по `~/personal-os-private/sources-private/`;
- НЕ публиковать в `site/` ничего без подтверждения владельца.

## 4. Жёсткие границы (как у hermes_a)

- Не коммить `learning.db`, нормативку, логи, ключи, черновики needs_manual_review.
- Не выдумывать ссылки на законы/СНиП.
- Сомневаешься — `needs_manual_review` + вопрос владельцу.
- Репозиторий публичный — приватное только в `~/personal-os-private/`.

## 5. Твой дашборд/инфраструктура (не связано с этим репо)

Твой дашборд на 9119 и прочая инфраструктура продолжают работать как раньше.
Этот репозиторий их больше не касается.

## 6. Полный контекст

Мастер-промпт системы — в `AGENTS.md` (для hermes_a) и у владельца.
Основные правила и структура описаны там же — читай перед работой.
